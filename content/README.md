You need to symlink your Obsidian vault into `./content/obsidian-vault`.

Then when you run the `deno run build` command the normalized
content folder will be generated from that and added as a sibling
folder to it.